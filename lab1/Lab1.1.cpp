// Write a C program to find maximum between two numbers using conditional operators
#include <stdio.h>

int main(){
    char c;
    printf("Enter a character : ");
    scanf("%c", &c);
    printf("ASCII code of the character entered is %d", c);

    return 0;
}